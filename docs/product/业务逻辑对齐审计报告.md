# 问玄东方全端一致性审计报告

> 审计日期：2026-07-29
> 范围：需求 PDF、产品原型、五个正式客户端、备用客户端、20 个后端服务、数据库/迁移、中间件脚本与 34 份 Markdown（其中 24 份架构文档）
> 当前结论：本地核心业务可继续联调，但不能声明“所有功能和生产能力全部对齐”

## 1. 总体判断

《需求文档-App改进》的六项改进都已具备原型落点、正式客户端入口、后端接口和数据库结构。预约计价、结构化时段、容量占位、gRPC 校验和本地模拟支付也已形成主闭环。

本次全面审查同时发现并修复了会造成错误结论的问题：网关管理角色越权、DIY 运行时样例数据回退、DIY 加持服务管理与计价数据分叉、制作完成与加持任务非事务、法师服务自动完成加持并生成假凭证、DIY 假微信/支付宝入口、预约完成后 C 端无评价入口、macOS 下 OpenIM 启动进程不能稳定脱离终端，以及多份文档仍称业务数据在内存中。

当前仍有生产阻断项和次要页面缺口，因此本报告不恢复“代码、原型和文档全部一致且可保证所有模块正常”的结论。

## 2. 三者关系

| 层级 | 角色 | 当前状态 |
|------|------|----------|
| 需求/PDF | 定义六项改进及边界 | 4 页已渲染复核，内容清晰 |
| 产品原型 | 定义用户入口、页面状态和操作路径 | 109 个 HTML、1614 个站内相对引用、缺失目标 0；DIY 实时串珠、模拟/关闭态均已显式呈现 |
| 客户端代码 | 执行用户操作并消费真实 API | 五个正式客户端均可构建；寺院端已补加持任务列表/详情；仍有少数 C 端二级入口不完整 |
| 后端代码 | 权威计价、状态机、持久化、鉴权和集成 | 20 个服务；核心数据已持久化，部分报表和外部 Provider 未生产化 |
| 文档 | 记录已实现契约、限制和部署方式 | 34 份 Markdown 已复核；API Reference 的 265 个唯一运行时 HTTP 契约与源码扫描结果一致；[功能对齐矩阵](./功能对齐矩阵.md)是功能总表 |

## 3. 六项改进矩阵

| 需求 | 原型/客户端 | 服务与数据 | 审计结果 |
|------|-------------|------------|----------|
| 信仰流派 | C 端专题、寺院/大师筛选；寺院台/平台台维护 | temple/master `beliefCode`，保留 `sect` 兼容 | 本地实现对齐 |
| 诉求聚合 | C 端混排商品和寺院服务 | product `/intentions` + 标签映射，不复制订单域 | 本地实现对齐 |
| AI 问事 | C 端直接对话、历史、新建和失败重试 | MySQL 会话/消息 + Provider | mock 仅开发；生产模型待验收 |
| 设计广场下单 | C 端原生 2.5D 实时串珠、拖动换位/删除、线材/腕围、撤销重做、草稿、真实地址、动态加持、支付结果和订单详情 | v2 有序设计文档；服务端材料/SKU 查价、库存事务、不可变快照、收益记录 | 原型/客户端/契约本地对齐；本地 mock 支付闭环，真实支付未接 |
| 视频/直播 | C 端播放、法师端上传、关闭态 | media-service + MinIO；直播 Provider 抽象 | 本地媒体可用；生产直播关闭 |
| 大师广场 | C 端混合内容，法师发布，平台审核 | community 表、审核、先审后显、幂等点赞 | 主链路对齐 |

## 4. 预约与加持专项

预约创建以 JWT 用户为准，booking-service 通过 `temple.rpc:9083`、`master.rpc:9084` 和 `payment.rpc:9090` 获取权威服务/法师/支付信息。服务费与功德金由服务端形成价格快照；日期时段使用条件更新占位，取消和支付超时释放一次；本地 `mock` 支付成功后进入寺院待确认。

DIY 制作完成时使用下单保存的设计快照判断是否加持。服务上架校验、订单状态和 `blessing_task` 创建在同一事务提交；法师必须在工作台执行“接受、开始、上传真实凭证并完成”，MQ 消费不再自动完成任务。

DIY 编辑器以 `designData.version=2` 保存腕围、余量、线材和有序 `beads[]`，并保留聚合 `items[]` 兼容设计广场下单。客户端价格只作即时预估；下单仍由 diy-service 按材料/SKU、上下架和库存重新计价。原型与 iOS 共用 14 项开发材料基线，默认示例为 16 颗、154 mm、含线材预估 ¥396。

预约评价的权威写入口是 `/api/v1/bookings/:id/review`，校验 JWT 所有权和 completed 状态。booking-service 持久化后向 review-service 同步读模型；MQ 不可用时读模型可能延迟，当前尚无事务型 outbox。

## 5. 已确认差异

| 编号 | 风险 | 差异 | 发布影响 |
|------|------|------|----------|
| F1 | 高 | auth-service 仍使用固定开发验证码 `1234`，历史密码为明文兼容 | 禁止生产发布；需短信 Provider、验证码 Redis 生命周期和密码哈希迁移 |
| F2 | 高 | payment-service 只实现 `mock`；微信/支付宝没有 SDK、签名与真实回调 | 服务已改为非 mock/生产配置拒绝启动 |
| F3 | 高 | 寺院报表把加持任务当预约并估算金额；商城趋势/热销商品仍为样例数据 | 报表不能用于经营决策，需事件读模型或报表 RPC |
| F4 | 中 | 物流批量同步和提现打款为本地模拟 Provider | 生产需快递和代付 Provider |
| F5 | 中 | AI 默认 mock、直播默认关闭 | 配置外部账号后必须单独做生产验收 |
| F6 | 中 | 评价同步依赖 best-effort MQ，没有 outbox/补偿扫描 | booking 评价不丢，通用评价列表可能短时缺项 |
| F7 | 中 | C 端收藏、账户安全、功德值/积分资产接口尚不完整；大师资料内商品/视频二级页仍有占位 | 不影响六项主链路，但页面覆盖不完整 |
| F8 | 中 | 平台备份仅支持手动全量；没有自动/增量/保留策略和自动维护模式 | 原型与文档已降级为真实能力 |
| F9 | 中 | 下游管理接口依赖网关注入角色头；直接暴露服务端口会绕过统一边界 | 生产网络只应暴露 gateway/OpenIM/对象存储必要入口 |

## 6. 数据与逻辑质量

- 预约、DIY、商城、支付、评价、审核、财务和营销主实体使用 MySQL，不再以内存 Map 作为事实源。
- DIY 材料和加持服务管理与计价读取同一业务库；C 端接口失败时不再展示样例订单或地址。
- 网关对 `platform_super`、`shop_admin`、`temple_admin`、`master` 做路径级角色校验，平台超管保留全局覆盖。
- 前向迁移在增加唯一键前先处理存量重复数据；`blessing_task.diy_order_no` 新增唯一约束防重复任务。
- 支付金额仍由订单域校验，客户端金额和用户 ID 不作为权威依据。

## 7. 验证记录

| 项目 | 结果 |
|------|------|
| 产品原型 | 109 页、1614 个本地引用、0 个缺失目标；总入口计数已修正，法师预约页覆盖确认/开始/完成 |
| PDF | 4 页渲染和视觉复核通过 |
| 接口契约 | 262 条 `.api` 声明 + 3 条显式注册路由 = 265 个唯一运行时 HTTP 契约；API Reference 缺失 0、过期 0 |
| Go | 21 个 go.work module 已逐模块测试通过；DIY v2 有序设计解析新增回归测试 |
| iOS | C 端 6 个 DIY 编辑器测试全部通过；C 端和法师端 Simulator 无签名构建通过；项目自身 actor 隔离和旧 `onChange` 警告已清零，剩余为 OpenIM Pod/Xcode 元数据警告 |
| Web | 三个 Vue 管理台生产构建通过；当前未启动三个 Vite 开发服务器，不属于构建失败 |
| Mock/备用端 | Mock TypeScript 构建与 `mobile-customer` 类型检查通过 |
| 数据一致性 | 预约计价/库存、支付金额、商城明细、DIY 计价、加持关联与凭证、非负价格库存、评论状态共 13/13 通过 |
| 运行时 | askXuan Compose 25 个服务/中间件全部 healthy；6 个 zrpc key、网关与 OpenIM 真实 readiness 通过；全栈检查 53/53 |

## 8. 后续顺序

1. 建立 booking/order/finance 报表读模型，删除寺院和商城报表样例数据。
2. 完成短信验证码、密码哈希迁移和生产认证配置校验。
3. 接入真实支付、快递、代付 Provider，并补签名、回调、重放和对账测试。
4. 补 C 端账户资产/安全和大师资料二级页。
5. 为评价等关键 MQ 增加 outbox 与补偿扫描。
6. 完成自动/增量备份、恢复演练和生产网络隔离后再评估生产发布。

只有 F1-F3 关闭，且完整运行时验收通过后，才可以重新给出“全端全部对齐并可保证功能正常”的结论。
