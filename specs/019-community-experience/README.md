---
status: complete
created: '2026-09-10'
tags: []
priority: high
created_at: '2026-09-10T16:35:20.455Z'
updated_at: '2026-09-10T17:41:57.804Z'
transitions:
  - status: in-progress
    at: '2026-09-10T16:35:48.302Z'
  - status: complete
    at: '2026-09-10T17:41:57.804Z'
completed_at: '2026-09-10T17:41:57.804Z'
completed: '2026-09-10'
---

# AI 阅读、私聊角标与大师广场体验

> **Status**: ✅ Complete · **Priority**: High · **Created**: 2026-09-10


## 范围与隔离

从 2026-09-11 当前代码继续，使用独立 Git worktree 和功能分支实现。原会话正在开发体验商城，本任务不编辑其工作区，不改商品、购物车、订单、支付、财务与共享图片组件。发布候选必须包含原会话最新已提交的商城成果；静态发布前校验线上目录未变更，保留商城/寺院兼容入口。

## 设计与实现

- H5 AI 对话与完整报告共用安全 CommonMark/GFM 渲染，支持标题、强调、列表、引用、表格、代码和链接；流式片段可继续渲染。禁止解释原始 HTML，不自动加载模型提供的远程图片。用户输入保留原文。iOS 共用分块 Markdown 阅读组件。
- H5 私聊角标原来取自 `/messages/unread-count`，因此站内通知 16 条会误显示成私聊 16。改为只聚合去重后的会话 `unreadCount`；后端未提供时不显示未知数量。双会话接口都失败时显示重试，不能显示为没有会话。
- H5 对话内直接嵌入广场。发现、关注、图文、视频、关键词和最新/热门筛选写入 URL；双列内容卡片、作者入口、图片放大/滑动、沉浸视频、内容分享和分页评论形成浏览链路。请求隔离防止旧筛选污染新结果，点赞/关注/评论防重复提交。不可播放媒体明确降级；视频不会被当作封面图片请求。
- 关注分类对游客显示就地登录引导，不请求受保护的数据；直播列表仅在登录后加载。登录过期时同时清除持久角色缓存，避免登录页被旧角色反复带回首页。
- iOS 广场同步筛选、搜索、分页、作者名称、点赞/关注状态、评论分页及提交锁。视频播放器保持生命周期，离开页面或应用后台时暂停。
- 作者工作台支持已上传素材预览、排序、删除、编辑回填和数量限制；发布失败保留编辑内容。
- 后台帖子审核提供图文/视频实际素材预览。审核通过时，在同一事务中更新帖子、审核记录及所属媒体的审核状态，避免帖子通过而附件不可见。数据库迁移只为 `community_user` 增加媒体 `audit_status`、`update_time` 列更新权，不创建示例内容，不改商城数据。

## 接口约定

`GET /api/v1/community/feed` 新增 `following`、`keyword`、`sort`，保留 `type/beliefCode/page/size`。`type=article|video`，兼容旧 H5 的 `image`；`sort=latest|popular`；关键词最多 80 字，SQL 参数化并转义 LIKE 通配符。关注过滤发生在分页前，身份只取可信请求头。响应新增 `following`、`coverUrl`，素材可包含 `url/coverUrl/duration`。普通媒体读取仍要求登录且 ready + approved 或归属本人，审核预览仅走平台角色校验后的后台接口。公开评论不能读取已下架帖子的评论或内部审核备注。

## 验证

- 社区服务 Go 测试覆盖筛选/分页、身份来源、批量点赞关注、媒体权限和审核事务回滚。
- 17 项浏览器回归通过：320/390/768 屏宽、图集、过滤竞争、未读数、点赞防重、评论降级和待审核语义、Markdown 注入/表格/报告目录、作者素材编辑、390/1280 后台预览，以及游客/登录过期。
- H5、平台后台生产构建和 iOS 模拟器构建均通过；iOS 未做真机分发。线上页面、Git 推送、ECS 容器健康及真实业务数据分别验收。

## 发布记录

2026-09-11，按用户明确授权向四个既有 GitHub 仓库推送独立分支 `codex/community-experience-20260911`，并在 ECS `101.96.228.71` 定向发布。原会话本地工作区和主分支未由本任务修改。

- H5 最终源码 `27736ca`，包含原会话商城最新 `f735f31`。前端/iOS 实现 `58b8b18`，后续仅增加回归用例至 `43951ef`。社区服务源码 `f3bdbd2`。
- 社区发布 `20260911-community-f3bdbd2`；静态最终发布 `20260911-community-web-27736ca`，部署标记与最终公网资源一致性检查通过。
- 发布脚本保存精确社区容器配置与镜像、社区/媒体数据库备份及前一静态指针。只重建社区服务；其他 34 个容器未变化，商城/寺院静态目录逐文件一致。
- 公网 H5 与后台 HTML、入口 JavaScript 的内容与发布候选字节一致，JavaScript MIME 正确，HTML 禁用缓存。网关健康、新筛选接口和关注接口鉴权正常。
- 浏览器确认新版页面、分类切换、搜索/清空搜索、游客提示稳定停留和 390 像素无横向溢出。线上目前没有已审核动态，完整图文/视频、点赞、评论和审核预览使用隔离测试数据验收，没有向线上写入示例动态或模拟互动。

本地验收材料：`/Users/gaofeng/develop/DongFang/artifacts/community-experience-20260911/REVIEW.md`，包含截图、最终构建/回归日志、线上验证记录及提交清单。
